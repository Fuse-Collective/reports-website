---
import PreviewLayout from '../../layouts/PreviewLayout.astro';
import Button from '../../components/ui/Button.astro';
import SectionIntro from '../../components/ui/SectionIntro.astro';
import Logo from '../../components/ui/Logo.astro';
import Badge from '../../components/ui/Badge.astro';
import Bullet from '../../components/ui/Bullet.astro';
import Table from '../../components/ui/Table.astro';
import AnalysisHeader from '../../components/common/AnalysisHeader.astro';
import InsightRow from '../../components/common/InsightRow.astro';
import RiskCard from '../../components/common/RiskCard.astro';
import ReportAreasMenu from '../../components/common/ReportAreasMenu.astro';
import IconBadge from '../../components/ui/IconBadge.astro';
import ApproachCompareCard from '../../components/common/ApproachCompareCard.astro';
import StatCircle from '../../components/common/StatCircle.astro';
import StatsGrid from '../../components/common/StatsGrid.astro';
import { Icon } from 'astro-icon/components';

const surfaces = ['light', 'muted', 'dark', 'accent'] as const;
const icons = ['none', 'arrow', 'padlock', 'book'] as const;
const btnSurface = (s: string) => s === 'muted' ? 'light' : s as 'light' | 'dark' | 'accent';

const problemIcons = [
  'problems/wizerunek',
  'problems/niespojnosc',
  'problems/brak-wyroznienia',
  'problems/brak-wytycznych',
  'problems/brak-porzadku',
  'problems/zmiany-strategiczne',
] as const;

const areaIcons = [
  'areas/target-click',
  'areas/eye-scan',
  'areas/dna-helix',
  'areas/shopping-bag',
  'areas/magic-wand',
  'areas/robot',
] as const;
---

<PreviewLayout title="UI Components" current="/design-system/components/">

  <!-- ===================== BUTTON ===================== -->
  <div class="py-comfortable px-page-gutter border-b border-outline-light" id="button">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.1 Button</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/ui/Button.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Variants: <code class="font-mono">primary</code>, <code class="font-mono">secondary</code>, <code class="font-mono">text-link</code>. Icon slot: right (pill) or left (text-link).</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">variant</td><td class="py-tight pr-relaxed text-text-muted">"primary" | "secondary" | "text-link"</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Filled, outline, or inline link</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">surface</td><td class="py-tight pr-relaxed text-text-muted">"light" | "accent" | "dark"</td><td class="py-tight pr-relaxed text-text-muted">"light"</td><td class="py-tight font-body text-text-muted">Background context</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">href</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">"#"</td><td class="py-tight font-body text-text-muted">URL</td></tr>
        <tr><td class="py-tight pr-relaxed">slot="icon"</td><td class="py-tight pr-relaxed text-text-muted">SVG</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Icon right (pill) / left (text-link)</td></tr>
      </tbody>
    </table>

    <div class="ds-component" data-active-surface="light" data-active-icon="none">
      {/* Switcher row */}
      <div class="flex flex-wrap gap-relaxed mb-compact items-center">
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Surface</span>
          {surfaces.map((s, i) => (
            <button data-dim="surface" data-value={s} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{s}</button>
          ))}
        </div>
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Icon</span>
          {icons.map((ic, i) => (
            <button data-dim="icon" data-value={ic} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{ic}</button>
          ))}
        </div>
      </div>

      {/* Panels — one per surface × one row per icon */}
      {surfaces.map((s) => (
        <div class:list={["ds-panel p-comfortable", s !== 'light' && 'hidden', `bg-surface-${s}`]} data-panel-surface={s} data-surface={s}>

          {/* Column headers */}
          <div class="grid grid-cols-3 gap-comfortable mb-compact">
            <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">Primary</p>
            <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">Secondary</p>
            <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">Text-link</p>
          </div>

          {/* Row: no icon */}
          <div class="grid grid-cols-3 gap-comfortable items-start" data-row-icon="none">
            <div><Button variant="primary" surface={btnSurface(s)}>Tekst buttona</Button></div>
            <div><Button variant="secondary" surface={btnSurface(s)}>Tekst buttona</Button></div>
            <div><Button variant="text-link" surface={btnSurface(s)}>Tekst linka</Button></div>
          </div>

          {/* Row: arrow */}
          <div class="grid grid-cols-3 gap-comfortable items-start hidden" data-row-icon="arrow">
            <div>
              <Button variant="primary" surface={btnSurface(s)}>
                Realizacje
                <svg slot="icon" viewBox="0 0 67 64" fill="none"><path d="M20.97 16.97L49.25 16.97V45.25" stroke="currentColor" stroke-width="2" stroke-linecap="square"/><path d="M18.14 48.08L47.48 18.75" stroke="currentColor" stroke-width="2" stroke-linecap="square" stroke-linejoin="round"/></svg>
              </Button>
            </div>
            <div>
              <Button variant="secondary" surface={btnSurface(s)}>
                Portfolio
                <svg slot="icon" viewBox="0 0 67 64" fill="none"><path d="M20.97 16.97L49.25 16.97V45.25" stroke="currentColor" stroke-width="2" stroke-linecap="square"/><path d="M18.14 48.08L47.48 18.75" stroke="currentColor" stroke-width="2" stroke-linecap="square" stroke-linejoin="round"/></svg>
              </Button>
            </div>
            <div>
              <Button variant="text-link" surface={btnSurface(s)}>
                Rebranding
                <svg slot="icon" viewBox="0 0 64 64" fill="none"><path d="M32 22.31C32 20.26 32.82 18.29 34.27 16.83C35.72 15.38 37.69 14.56 39.75 14.56H55.25C55.76 14.56 56.26 14.77 56.62 15.13C56.98 15.49 57.19 15.99 57.19 16.5V47.5C57.19 48.01 56.98 48.51 56.62 48.87C56.26 49.23 55.76 49.44 55.25 49.44H39.75C37.69 49.44 35.72 50.25 34.27 51.71C32.82 53.16 32 55.13 32 57.19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><path d="M6.81 47.5C6.81 48.01 7.02 48.51 7.38 48.87C7.74 49.23 8.24 49.44 8.75 49.44H24.25C26.31 49.44 28.28 50.25 29.73 51.71C31.18 53.16 32 55.13 32 57.19V22.31C32 20.26 31.18 18.29 29.73 16.83C28.28 15.38 26.31 14.56 24.25 14.56H8.75C8.24 14.56 7.74 14.77 7.38 15.13C7.02 15.49 6.81 15.99 6.81 16.5V47.5Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
              </Button>
            </div>
          </div>

          {/* Row: padlock */}
          <div class="grid grid-cols-3 gap-comfortable items-start hidden" data-row-icon="padlock">
            <div>
              <Button variant="primary" surface={btnSurface(s)}>
                Odblokuj
                <svg slot="icon" viewBox="0 0 64 64" fill="none"><rect x="14" y="32" width="36" height="24" rx="2" stroke="currentColor" stroke-width="2"/><path d="M22 32V22C22 14.268 27.268 9 34 9V9C40.732 9 46 14.268 46 22V32" stroke="currentColor" stroke-width="2" stroke-linecap="square"/><circle cx="32" cy="44" r="3" stroke="currentColor" stroke-width="2"/></svg>
              </Button>
            </div>
            <div>
              <Button variant="secondary" surface={btnSurface(s)}>
                Odblokuj
                <svg slot="icon" viewBox="0 0 64 64" fill="none"><rect x="14" y="32" width="36" height="24" rx="2" stroke="currentColor" stroke-width="2"/><path d="M22 32V22C22 14.268 27.268 9 34 9V9C40.732 9 46 14.268 46 22V32" stroke="currentColor" stroke-width="2" stroke-linecap="square"/><circle cx="32" cy="44" r="3" stroke="currentColor" stroke-width="2"/></svg>
              </Button>
            </div>
            <div>
              <Button variant="text-link" surface={btnSurface(s)}>
                Odblokuj
                <svg slot="icon" viewBox="0 0 64 64" fill="none"><rect x="14" y="32" width="36" height="24" rx="2" stroke="currentColor" stroke-width="2"/><path d="M22 32V22C22 14.268 27.268 9 34 9V9C40.732 9 46 14.268 46 22V32" stroke="currentColor" stroke-width="2" stroke-linecap="square"/><circle cx="32" cy="44" r="3" stroke="currentColor" stroke-width="2"/></svg>
              </Button>
            </div>
          </div>

          {/* Row: book */}
          <div class="grid grid-cols-3 gap-comfortable items-start hidden" data-row-icon="book">
            <div>
              <Button variant="primary" surface={btnSurface(s)}>
                Rebranding
                <svg slot="icon" viewBox="0 0 64 64" fill="none"><path d="M32 22.31C32 20.26 32.82 18.29 34.27 16.83C35.72 15.38 37.69 14.56 39.75 14.56H55.25C55.76 14.56 56.26 14.77 56.62 15.13C56.98 15.49 57.19 15.99 57.19 16.5V47.5C57.19 48.01 56.98 48.51 56.62 48.87C56.26 49.23 55.76 49.44 55.25 49.44H39.75C37.69 49.44 35.72 50.25 34.27 51.71C32.82 53.16 32 55.13 32 57.19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><path d="M6.81 47.5C6.81 48.01 7.02 48.51 7.38 48.87C7.74 49.23 8.24 49.44 8.75 49.44H24.25C26.31 49.44 28.28 50.25 29.73 51.71C31.18 53.16 32 55.13 32 57.19V22.31C32 20.26 31.18 18.29 29.73 16.83C28.28 15.38 26.31 14.56 24.25 14.56H8.75C8.24 14.56 7.74 14.77 7.38 15.13C7.02 15.49 6.81 15.99 6.81 16.5V47.5Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
              </Button>
            </div>
            <div>
              <Button variant="secondary" surface={btnSurface(s)}>
                Rebranding
                <svg slot="icon" viewBox="0 0 64 64" fill="none"><path d="M32 22.31C32 20.26 32.82 18.29 34.27 16.83C35.72 15.38 37.69 14.56 39.75 14.56H55.25C55.76 14.56 56.26 14.77 56.62 15.13C56.98 15.49 57.19 15.99 57.19 16.5V47.5C57.19 48.01 56.98 48.51 56.62 48.87C56.26 49.23 55.76 49.44 55.25 49.44H39.75C37.69 49.44 35.72 50.25 34.27 51.71C32.82 53.16 32 55.13 32 57.19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><path d="M6.81 47.5C6.81 48.01 7.02 48.51 7.38 48.87C7.74 49.23 8.24 49.44 8.75 49.44H24.25C26.31 49.44 28.28 50.25 29.73 51.71C31.18 53.16 32 55.13 32 57.19V22.31C32 20.26 31.18 18.29 29.73 16.83C28.28 15.38 26.31 14.56 24.25 14.56H8.75C8.24 14.56 7.74 14.77 7.38 15.13C7.02 15.49 6.81 15.99 6.81 16.5V47.5Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
              </Button>
            </div>
            <div>
              <Button variant="text-link" surface={btnSurface(s)}>
                Rebranding
                <svg slot="icon" viewBox="0 0 64 64" fill="none"><path d="M32 22.31C32 20.26 32.82 18.29 34.27 16.83C35.72 15.38 37.69 14.56 39.75 14.56H55.25C55.76 14.56 56.26 14.77 56.62 15.13C56.98 15.49 57.19 15.99 57.19 16.5V47.5C57.19 48.01 56.98 48.51 56.62 48.87C56.26 49.23 55.76 49.44 55.25 49.44H39.75C37.69 49.44 35.72 50.25 34.27 51.71C32.82 53.16 32 55.13 32 57.19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><path d="M6.81 47.5C6.81 48.01 7.02 48.51 7.38 48.87C7.74 49.23 8.24 49.44 8.75 49.44H24.25C26.31 49.44 28.28 50.25 29.73 51.71C31.18 53.16 32 55.13 32 57.19V22.31C32 20.26 31.18 18.29 29.73 16.83C28.28 15.38 26.31 14.56 24.25 14.56H8.75C8.24 14.56 7.74 14.77 7.38 15.13C7.02 15.49 6.81 15.99 6.81 16.5V47.5Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
              </Button>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>

  <!-- ===================== SECTION INTRO ===================== -->
  <div class="py-comfortable px-page-gutter border-b border-outline-light" id="section-intro">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.2 SectionIntro</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/ui/SectionIntro.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">50/50 header. Left: headline. Right: optional indicator + body.</p>

    <div class="ds-component" data-active-surface="light" data-active-indicator="right">
      <div class="flex flex-wrap gap-relaxed mb-compact items-center">
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Surface</span>
          {surfaces.map((s, i) => (
            <button data-dim="surface" data-value={s} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{s}</button>
          ))}
        </div>
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Indicator</span>
          {['right', 'left', 'none'].map((v, i) => (
            <button data-dim="indicator" data-value={v} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{v}</button>
          ))}
        </div>
      </div>

      {surfaces.map((s) => (
        <section class:list={["ds-panel p-comfortable", s !== 'light' && 'hidden', `bg-surface-${s}`]} data-panel-surface={s} data-surface={s}>
          <div data-row-indicator="right">
            <SectionIntro
              headline="Specjalizujemy się w kompleksowych projektach"
              body="Pracujemy z liderami lub pretendentami bardzo różnych branż nad rozwiązaniem złożonych problemów brandingowych."
              indicator="right"
            />
          </div>
          <div class="hidden" data-row-indicator="left">
            <SectionIntro
              headline="Specjalizujemy się w kompleksowych projektach"
              body="Pracujemy z liderami lub pretendentami bardzo różnych branż nad rozwiązaniem złożonych problemów brandingowych."
              indicator="left"
            />
          </div>
          <div class="hidden" data-row-indicator="none">
            <SectionIntro
              headline="Specjalizujemy się w kompleksowych projektach"
              body="Pracujemy z liderami lub pretendentami bardzo różnych branż nad rozwiązaniem złożonych problemów brandingowych."
            />
          </div>
        </section>
      ))}
    </div>
  </div>

  <!-- ===================== SECTION INTRO (stacked-left) ===================== -->
  <div class="py-comfortable px-page-gutter border-b border-outline-light" id="section-intro-stacked">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.3 SectionIntro — stacked-left</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/ui/SectionIntro.astro · layout="stacked-left"</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Renders only the stacked intro block (headline + optional indicator + body). The host wraps it in its own grid and places sibling content next to it — so the host controls column ratio and composition.</p>

    <div class="ds-component" data-active-surface="light" data-active-indicator="arrow">
      <div class="flex flex-wrap gap-relaxed mb-compact items-center">
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Surface</span>
          {surfaces.map((s, i) => (
            <button data-dim="surface" data-value={s} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{s}</button>
          ))}
        </div>
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Indicator</span>
          {['arrow', 'left', 'none'].map((v, i) => (
            <button data-dim="indicator" data-value={v} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{v}</button>
          ))}
        </div>
      </div>

      {surfaces.map((s) => (
        <section class:list={["ds-panel p-comfortable", s !== 'light' && 'hidden', `bg-surface-${s}`]} data-panel-surface={s} data-surface={s}>

          <p class="font-body text-label-sm text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50 mb-compact">
            Standalone (host-wraps into grid):
          </p>

          <div data-row-indicator="arrow">
            <div class="grid grid-cols-1 lg:grid-cols-[2fr_3fr] gap-comfortable lg:gap-monumental mb-comfortable">
              <SectionIntro
                headline="Obserwujesz u siebie te problemy?"
                body="Od ponad 10 lat pomagamy je rozwiązywać, tworząc skuteczne strategie marek oraz wyróżniające identyfikacje wizualne."
                layout="stacked-left"
                indicator="arrow"
              />
              <div class="flex items-center justify-center border border-dashed border-outline-light on-dark:border-on-surface-light/30 on-accent:border-on-surface-dark/30 p-relaxed">
                <span class="font-body text-label-sm text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">host content (grid/cards/etc.)</span>
              </div>
            </div>
          </div>

          <div class="hidden" data-row-indicator="left">
            <div class="grid grid-cols-1 lg:grid-cols-[2fr_3fr] gap-comfortable lg:gap-monumental mb-comfortable">
              <SectionIntro
                headline="Obserwujesz u siebie te problemy?"
                body="Od ponad 10 lat pomagamy je rozwiązywać, tworząc skuteczne strategie marek oraz wyróżniające identyfikacje wizualne."
                layout="stacked-left"
                indicator="left"
              />
              <div class="flex items-center justify-center border border-dashed border-outline-light on-dark:border-on-surface-light/30 on-accent:border-on-surface-dark/30 p-relaxed">
                <span class="font-body text-label-sm text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">host content</span>
              </div>
            </div>
          </div>

          <div class="hidden" data-row-indicator="none">
            <div class="grid grid-cols-1 lg:grid-cols-[2fr_3fr] gap-comfortable lg:gap-monumental mb-comfortable">
              <SectionIntro
                headline="Obserwujesz u siebie te problemy?"
                body="Od ponad 10 lat pomagamy je rozwiązywać, tworząc skuteczne strategie marek oraz wyróżniające identyfikacje wizualne."
                layout="stacked-left"
              />
              <div class="flex items-center justify-center border border-dashed border-outline-light on-dark:border-on-surface-light/30 on-accent:border-on-surface-dark/30 p-relaxed">
                <span class="font-body text-label-sm text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">host content</span>
              </div>
            </div>
          </div>

        </section>
      ))}
    </div>
  </div>

  <!-- ===================== ICONS ===================== -->
  <div class="py-comfortable px-page-gutter border-b border-outline-light" id="icons">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.4 Icons</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/icons/ · &lt;Icon name="..." /&gt;</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Custom SVG icons rendered via astro-icon. <code class="font-mono">currentColor</code> strokes adapt to surface text color; yellow fills stay as brand accent.</p>

    <div class="ds-component" data-active-surface="light">
      <div class="flex items-center gap-tight mb-compact">
        <span class="font-body text-label-sm text-text-muted">Surface</span>
        {surfaces.map((s, i) => (
          <button data-dim="surface" data-value={s} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{s}</button>
        ))}
      </div>

      {surfaces.map((s) => (
        <div class:list={["ds-panel p-comfortable", s !== 'light' && 'hidden', `bg-surface-${s}`]} data-panel-surface={s} data-surface={s}>

          <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50 mb-compact">Arrows</p>
          <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-comfortable mb-relaxed">
            <div class="flex flex-col gap-tight items-start">
              <Icon name="arrow-right-long" class="w-[160px] h-auto text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent" />
              <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/60 on-accent:text-on-surface-dark/60">arrow-right-long</code>
            </div>
            <div class="flex flex-col gap-tight items-start">
              <Icon name="indicator-circle" class="w-[80px] h-[80px] text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent" />
              <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/60 on-accent:text-on-surface-dark/60">indicator-circle</code>
            </div>
            <div class="flex flex-col gap-tight items-start">
              <Icon name="padlock" class="w-[64px] h-[64px] text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent" />
              <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/60 on-accent:text-on-surface-dark/60">padlock</code>
            </div>
          </div>

          <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50 mb-compact">Problems</p>
          <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-comfortable mb-relaxed">
            {problemIcons.map((name) => (
              <div class="flex flex-col gap-tight items-start">
                <Icon name={name} class="w-[120px] h-[120px] text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent" />
                <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/60 on-accent:text-on-surface-dark/60">{name}</code>
              </div>
            ))}
          </div>

          <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50 mb-compact">Area headers (full composition: dashed circle + yellow dot + Material Symbol)</p>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-comfortable">
            {areaIcons.map((name) => (
              <div class="flex flex-col gap-tight items-start">
                <Icon name={name} class="w-[258px] h-[149px] text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent" />
                <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/60 on-accent:text-on-surface-dark/60">{name}</code>
              </div>
            ))}
          </div>

        </div>
      ))}
    </div>
  </div>

  <!-- ===================== LOGO ===================== -->
  <div class="py-comfortable px-page-gutter" id="logo">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.5 Logo</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/ui/Logo.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Monochromatic SVG. "Fuse" center is a hole — background shows through.</p>

    <div class="ds-component" data-active-surface="light">
      <div class="flex items-center gap-tight mb-compact">
        <span class="font-body text-label-sm text-text-muted">Surface</span>
        {surfaces.map((s, i) => (
          <button data-dim="surface" data-value={s} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{s}</button>
        ))}
      </div>

      {surfaces.map((s) => {
        const v = (s === 'dark' ? 'light' : 'dark') as 'dark' | 'light' | 'accent';
        return (
          <div class:list={["ds-panel p-comfortable", s !== 'light' && 'hidden', `bg-surface-${s}`]} data-panel-surface={s}>
            <div class="flex flex-wrap gap-relaxed items-end">
              {[40, 56, 80, 120].map((size) => (
                <div class="flex flex-col gap-tight items-start">
                  <Logo variant={v} size={size} />
                  <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">{size}px</code>
                </div>
              ))}
            </div>
          </div>
        );
      })}
    </div>
  </div>

  <!-- ===================== BADGE ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="badge">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.6 Badge</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/ui/Badge.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Circular icon container. Variants: <code class="font-mono">filled</code>, <code class="font-mono">outlined</code>, <code class="font-mono">naked</code>. Sizes: sm 24 · md 56 · lg 84 · xl 148. Surface drives fill/border/icon color.</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">icon</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">astro-icon name</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">size</td><td class="py-tight pr-relaxed text-text-muted">"sm" | "md" | "lg" | "xl"</td><td class="py-tight pr-relaxed text-text-muted">"md"</td><td class="py-tight font-body text-text-muted">24 · 56 · 84 · 148</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">variant</td><td class="py-tight pr-relaxed text-text-muted">"filled" | "outlined" | "naked"</td><td class="py-tight pr-relaxed text-text-muted">"filled"</td><td class="py-tight font-body text-text-muted">Container style</td></tr>
        <tr><td class="py-tight pr-relaxed">surface</td><td class="py-tight pr-relaxed text-text-muted">"light" | "muted" | "accent" | "dark"</td><td class="py-tight pr-relaxed text-text-muted">"light"</td><td class="py-tight font-body text-text-muted">Color context</td></tr>
      </tbody>
    </table>

    <div class="ds-component" data-active-surface="light">
      <div class="flex items-center gap-tight mb-compact">
        <span class="font-body text-label-sm text-text-muted">Surface</span>
        {surfaces.map((s, i) => (
          <button data-dim="surface" data-value={s} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{s}</button>
        ))}
      </div>

      {surfaces.map((s) => (
        <div class:list={["ds-panel p-comfortable", s !== 'light' && 'hidden', `bg-surface-${s}`]} data-panel-surface={s} data-surface={s}>

          {/* Column headers */}
          <div class="grid grid-cols-3 gap-comfortable mb-compact">
            <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">Filled</p>
            <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">Outlined</p>
            <p class="font-body text-label-sm uppercase tracking-wide text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">Naked</p>
          </div>

          {/* Rows: one per size */}
          {(['sm', 'md', 'lg', 'xl'] as const).map((size) => (
            <div class="grid grid-cols-3 gap-comfortable items-center mb-standard">
              <div class="flex flex-col gap-tight items-start">
                <Badge icon="ads-click" size={size} variant="filled" surface={s} />
                <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">size={size}</code>
              </div>
              <div class="flex flex-col gap-tight items-start">
                <Badge icon="ads-click" size={size} variant="outlined" surface={s} />
                <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">size={size}</code>
              </div>
              <div class="flex flex-col gap-tight items-start">
                <Badge icon="ads-click" size={size} variant="naked" surface={s} />
                <code class="font-mono text-label-sm text-text-muted on-dark:text-on-surface-light/50 on-accent:text-on-surface-dark/50">size={size}</code>
              </div>
            </div>
          ))}
        </div>
      ))}
    </div>
  </div>

  <!-- ===================== TABLE ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="table">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.7 Table</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/ui/Table.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Comparison table. First column is the row-label (muted label style). Optional column highlight (yellow).</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">columns</td><td class="py-tight pr-relaxed text-text-muted">string[]</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Column headers (1st = row-label column)</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">rows</td><td class="py-tight pr-relaxed text-text-muted">string[][]</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Data rows (cells per row match columns)</td></tr>
        <tr><td class="py-tight pr-relaxed">highlightedColumnIndex</td><td class="py-tight pr-relaxed text-text-muted">number</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Column to highlight yellow (excl. first)</td></tr>
      </tbody>
    </table>

    <div class="ds-component space-y-comfortable">
      {/* Example 1: highlighted column */}
      <div class="p-comfortable bg-surface-light">
        <p class="font-body text-label-sm uppercase tracking-wide text-text-muted mb-compact">With highlighted column</p>
        <Table
          columns={['ASPEKT', 'Brandwatch', 'Meltwater', 'Brand24']}
          rows={[
            ['Wdrożenie', 'Dedykowane wdrożenie', 'Guided demo', 'Konfiguracja w minuty'],
            ['Wnioski', 'Panele danych', 'Panele danych', 'AI generuje gotowe'],
            ['Cennik', 'Brak publicznego', 'Brak publicznego', 'Publiczny'],
            ['Cena miesięczna', 'Indywidualna wycena', 'Indywidualna wycena', '$199/mies. (locale)'],
          ]}
          highlightedColumnIndex={3}
        />
      </div>

      {/* Example 2: no highlight */}
      <div class="p-comfortable bg-surface-light">
        <p class="font-body text-label-sm uppercase tracking-wide text-text-muted mb-compact">Without highlight</p>
        <Table
          columns={['METRYKA', 'Q1', 'Q2', 'Q3', 'Q4']}
          rows={[
            ['MRR', '120k', '180k', '240k', '310k'],
            ['ARPU', '$180', '$195', '$220', '$264'],
            ['NPS', '42', '48', '51', '56'],
          ]}
        />
      </div>
    </div>
  </div>

  <!-- ===================== BULLET ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="bullet">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.8 Bullet</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/ui/Bullet.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Square marker for unordered lists — two sizes: <code class="font-mono">lg</code> (12×12, default, paired with body text via <code class="font-mono">mt-[0.45em]</code>) and <code class="font-mono">sm</code> (4×4, for footnote-style lists, paired with <code class="font-mono">mt-[0.65em]</code>). Fill inherits the parent section's <code class="font-mono">data-surface</code> via <code class="font-mono">on-dark:</code> / <code class="font-mono">on-accent:</code> prefixes.</p>

    <div class="ds-component" data-active-surface="light">
      <div class="flex items-center gap-tight mb-compact">
        <span class="font-body text-label-sm text-text-muted">Surface</span>
        {surfaces.map((s, i) => (
          <button data-dim="surface" data-value={s} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{s}</button>
        ))}
      </div>

      {surfaces.map((s) => (
        <div class:list={["ds-panel p-comfortable flex flex-col gap-relaxed", s !== 'light' && 'hidden', `bg-surface-${s}`]} data-panel-surface={s} data-surface={s}>
          <div class="flex flex-col gap-tight">
            <span class="font-body text-label-sm text-text-muted on-dark:text-on-surface-light/70 on-accent:text-on-surface-accent/70">Large (12px) — default</span>
            <ul class="flex flex-col gap-compact m-0 p-0 list-none max-w-[720px]">
              <li class="flex gap-standard items-start">
                <Bullet size="lg" class="mt-[calc(0.45em+1px)]" />
                <span class="font-body text-body font-normal leading-normal text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent">
                  Pierwszy punkt — bullet dziedziczy kolor z otaczającej sekcji.
                </span>
              </li>
              <li class="flex gap-standard items-start">
                <Bullet size="lg" class="mt-[calc(0.45em+1px)]" />
                <span class="font-body text-body font-normal leading-normal text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent">
                  <strong>Drugi punkt:</strong> bold fragment to wzorzec z YAML (rich text przez <code class="font-mono">set:html</code>).
                </span>
              </li>
              <li class="flex gap-standard items-start">
                <Bullet size="lg" class="mt-[calc(0.45em+1px)]" />
                <span class="font-body text-body font-normal leading-normal text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent">
                  Trzeci punkt — dłuższy tekst, żeby pokazać jak bullet wygląda obok wiersza, który się zawija na dwie linie przy węższym kontenerze.
                </span>
              </li>
            </ul>
          </div>

          <div class="flex flex-col gap-tight">
            <span class="font-body text-label-sm text-text-muted on-dark:text-on-surface-light/70 on-accent:text-on-surface-accent/70">Small (4px)</span>
            <ul class="flex flex-col gap-compact m-0 p-0 list-none max-w-[720px]">
              <li class="flex gap-tight items-start">
                <Bullet size="sm" class="mt-[0.65em]" />
                <span class="font-body text-body-sm font-normal leading-normal text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent">
                  Mały wariant — dla list drugorzędnych lub w przypisach.
                </span>
              </li>
              <li class="flex gap-tight items-start">
                <Bullet size="sm" class="mt-[0.65em]" />
                <span class="font-body text-body-sm font-normal leading-normal text-on-surface-dark on-dark:text-on-surface-light on-accent:text-on-surface-accent">
                  Drugi punkt — dopasuj <code class="font-mono">mt</code> jeśli zmieniasz rozmiar tekstu.
                </span>
              </li>
            </ul>
          </div>
        </div>
      ))}
    </div>
  </div>

  <!-- ===================== ANALYSIS HEADER ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="analysis-header">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.9 AnalysisHeader</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/common/AnalysisHeader.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Area-analysis header: twin overlapping circles (dark outline + yellow fill) with a center icon, paired with H1 title + intro paragraph. Title splits on the first <code class="font-mono">:</code> — lead bold on line 1, rest on line 2 (colon dropped).</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">icon</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">astro-icon name (center badge)</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">title</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">First <code class="font-mono">:</code> splits into bold lead + rest</td></tr>
        <tr><td class="py-tight pr-relaxed">intro</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Intro paragraph under header</td></tr>
      </tbody>
    </table>

    <div class="ds-component" data-active-title="split">
      <div class="flex flex-wrap gap-relaxed mb-compact items-center">
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Title</span>
          {(['split', 'single'] as const).map((v, i) => (
            <button data-dim="title" data-value={v} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{v}</button>
          ))}
        </div>
      </div>

      <div class="p-comfortable bg-surface-light">
        <div data-row-title="split">
          <AnalysisHeader
            icon="areas/target-click"
            title="Bolączki kategorii: AI zamiast ręcznej analizy"
            intro="Kategoria social listening ma chroniczne problemy: narzędzia zbierają tysiące wzmianek, ale użytkownik sam musi wydobyć wnioski. Konfiguracja trwa tygodnie. Analiza sentymentu jest niedokładna."
          />
        </div>
        <div class="hidden" data-row-title="single">
          <AnalysisHeader
            icon="areas/eye-scan"
            title="Łamanie schematów kategorii"
            intro="Brand24 rozbija niepisane reguły branży: publiczny cennik, trial bez karty, miesięczne subskrypcje — w kategorii, która to wszystko ukrywa za formularzem „Request a demo”."
          />
        </div>
      </div>
    </div>
  </div>

  <!-- ===================== INSIGHT ROW ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="insight-row">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.10 InsightRow</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/common/InsightRow.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Single "Główne wnioski" row. Asymmetric grid <code class="font-mono">360fr / 40px / 532fr</code> per Figma. Title right-aligned at lg+. Decorative vertical pill (dark-outlined capsule 40×116 with yellow circle) between columns.</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">title</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Short headline (wraps ~2 lines in narrow column)</td></tr>
        <tr><td class="py-tight pr-relaxed">description</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Body paragraph</td></tr>
      </tbody>
    </table>

    <div class="p-comfortable bg-surface-light">
      <p class="font-body text-label-sm uppercase tracking-wide text-text-muted mb-compact">3 stacked rows</p>
      <div class="flex flex-col">
        <InsightRow
          title="AI zamiast surowych danych"
          description="Konkurencja daje panele danych. Brand24 daje odpowiedzi. AI Brand Assistant pozwala zadać pytanie w języku naturalnym i dostać gotowy wniosek. AI Insights generuje automatyczne raporty z trendami."
        />
        <InsightRow
          title="Sentyment z 95% skutecznością"
          description={"AI Sentiment Analysis osiąga 95% F1 score na próbie 50 tys. wzmianek. Konkurencja? \"Niedokładny sentyment\" to stały motyw w recenzjach Brandwatch i Meltwater."}
        />
        <InsightRow
          title="Konfiguracja w minuty"
          description="Ocena łatwości obsługi: 4,7/5 na bazie 250+ recenzji. Brandwatch wymaga dedykowanego wdrożenia. Meltwater prowadzi przez demo. Brand24? Wpisujesz słowo kluczowe i zaczynasz."
        />
      </div>
    </div>
  </div>

  <!-- ===================== RISK CARD ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="risk-card">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.11 RiskCard</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/common/RiskCard.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Muted-background card for "Gdzie leży ryzyko?". Fixed internal <code class="font-mono">bg-surface-muted</code>, 60px padding, 48px gap. Icon rendered via <code class="font-mono">Badge</code> (size <code class="font-mono">lg</code>, variant <code class="font-mono">naked</code>).</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">icon</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">astro-icon name</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">title</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Risk name</td></tr>
        <tr><td class="py-tight pr-relaxed">body</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Risk description</td></tr>
      </tbody>
    </table>

    <div class="p-comfortable bg-surface-light">
      <p class="font-body text-label-sm uppercase tracking-wide text-text-muted mb-compact">2-column grid (how they appear in AnalysisSection)</p>
      <div class="grid gap-standard lg:grid-cols-2 lg:gap-relaxed">
        <RiskCard
          icon="control-point-duplicate"
          title="Kopiowalność"
          body="Trudna w krótkim terminie. Wymaga połączenia danych (25 mln źródeł), UX, AI osadzonego w przepływie pracy i tempa wydawania nowych wersji. Kopiowalna w średnim terminie przez duże platformy, jeśli uznają segment MŚP za strategiczny."
        />
        <RiskCard
          icon="visibility-lock"
          title="Ograniczenie kategorii"
          body="Niedokładny sentyment to systemowa wada całej branży, nie specyficzny problem Brand24. Żadne narzędzie social listening nie rozwiązało tego w pełni. Szum w wynikach występuje u wszystkich graczy."
        />
      </div>
    </div>
  </div>

  <!-- ===================== REPORT AREAS MENU ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="report-areas-menu">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.12 ReportAreasMenu</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/common/ReportAreasMenu.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">6-area TOC menu used inline in OverviewSection and in the page-level sticky overlay. Each <code class="font-mono">&lt;li&gt;</code> carries <code class="font-mono">data-area-number</code>; page-level scroll-spy toggles <code class="font-mono">data-current="true"</code> on the matching item. Both visual variants (current = dark circle + bold / inactive = small number + label) are rendered per item; CSS shows one or the other via <code class="font-mono">group-data-[current=true]:</code>.</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">areasLabel</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Pill label above the list</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">areas</td><td class="py-tight pr-relaxed text-text-muted">Area[]</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">number, label, state, optional href</td></tr>
        <tr><td class="py-tight pr-relaxed">sectionPills</td><td class="py-tight pr-relaxed text-text-muted">SectionPill[]</td><td class="py-tight pr-relaxed text-text-muted">[]</td><td class="py-tight font-body text-text-muted">Locked-section chips below the list</td></tr>
      </tbody>
    </table>

    <div class="ds-component" data-active-surface="accent" data-active-current="none">
      {/* Switchers */}
      <div class="flex flex-wrap gap-relaxed mb-compact items-center">
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Surface</span>
          {surfaces.map((s, i) => (
            <button data-dim="surface" data-value={s} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", s === 'accent' ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{s}</button>
          ))}
        </div>
        <div class="flex items-center gap-tight">
          <span class="font-body text-label-sm text-text-muted">Active area</span>
          {(['none', '01', '02', '03', '04', '05', '06'] as const).map((n, i) => (
            <button data-dim="current" data-value={n} class:list={["px-compact py-tight font-body text-label-sm border border-outline-light rounded-button cursor-pointer", i === 0 ? "bg-on-surface-dark text-on-surface-light" : "hover:bg-surface-muted"]}>{n}</button>
          ))}
        </div>
      </div>

      {/* Panels — one per surface, menu rendered identically in each */}
      {surfaces.map((s) => (
        <div class:list={["ds-panel p-comfortable", s !== 'accent' && 'hidden', `bg-surface-${s}`]} data-panel-surface={s} data-surface={s}>
          <div class="max-w-[320px]">
            <ReportAreasMenu
              areasLabel="6 obszarów przewagi"
              areas={[
                { number: '01', label: 'Bolączki kategorii', state: 'done' },
                { number: '02', label: 'Łamanie schematów', state: 'done' },
                { number: '03', label: 'DNA firmy', state: 'done' },
                { number: '04', label: 'Demokratyzacja', state: 'done' },
                { number: '05', label: 'Kombinacje wartości', state: 'done' },
                { number: '06', label: 'Nowa podkategoria', state: 'done' },
              ]}
              sectionPills={[
                { label: 'Konstelacje przewag', state: 'locked' },
                { label: 'Ocena strategiczna', state: 'locked' },
              ]}
            />
          </div>
        </div>
      ))}
    </div>
  </div>

  <!-- ===================== ICON BADGE ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="icon-badge">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.13 IconBadge</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/ui/IconBadge.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Pure decorative circular disk. Rendered as a bare <code class="font-mono">&lt;span&gt;</code> with background colour and border radius — no content slot. Callers stack glyphs or text over it via a relative wrapper. Used by <code class="font-mono">ApproachCompareCard</code> (md, overflowing glyph) and <code class="font-mono">StatCircle</code> (lg, centred value).</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">size</td><td class="py-tight pr-relaxed text-text-muted">"md" | "lg"</td><td class="py-tight pr-relaxed text-text-muted">"md"</td><td class="py-tight font-body text-text-muted">64px (md) or 270px (lg)</td></tr>
        <tr><td class="py-tight pr-relaxed">tone</td><td class="py-tight pr-relaxed text-text-muted">"accent" | "neutral"</td><td class="py-tight pr-relaxed text-text-muted">"accent"</td><td class="py-tight font-body text-text-muted">Yellow (accent) or grey (neutral)</td></tr>
      </tbody>
    </table>

    <div class="p-comfortable bg-surface-light flex items-end gap-comfortable flex-wrap">
      <div class="flex flex-col gap-tight items-center"><IconBadge size="md" tone="neutral" /><span class="font-body text-label-sm text-text-muted">md / neutral</span></div>
      <div class="flex flex-col gap-tight items-center"><IconBadge size="md" tone="accent" /><span class="font-body text-label-sm text-text-muted">md / accent</span></div>
      <div class="flex flex-col gap-tight items-center"><IconBadge size="lg" tone="accent" /><span class="font-body text-label-sm text-text-muted">lg / accent</span></div>
    </div>
  </div>

  <!-- ===================== APPROACH COMPARE CARD ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="approach-compare-card">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.14 ApproachCompareCard</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/common/ApproachCompareCard.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">One column of a before/after approach comparison. Header is a face glyph overlapping an <code class="font-mono">IconBadge</code> (64px disk + 60px glyph offset right); below it an h4 label and body description. Reusable for typical-vs-ours, old-vs-new, problem-vs-solution pairings.</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">icon</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">astro-icon name, e.g. "approaches/face-happy"</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">tone</td><td class="py-tight pr-relaxed text-text-muted">"accent" | "neutral"</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Disk colour under the glyph</td></tr>
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">label</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">h4 headline</td></tr>
        <tr><td class="py-tight pr-relaxed">description</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Body copy</td></tr>
      </tbody>
    </table>

    <div class="p-comfortable bg-surface-light grid grid-cols-1 lg:grid-cols-2 gap-comfortable lg:gap-[120px]">
      <ApproachCompareCard
        icon="approaches/face-neutral"
        tone="neutral"
        label="Typowe podejście"
        description="Szukasz jednego USP. Jednej rzeczy, która Cię wyróżnia. Problem: jedno USP jest kopiowalne."
      />
      <ApproachCompareCard
        icon="approaches/face-happy"
        tone="accent"
        label="Nasze podejście"
        description="Szukamy konstelacji. Grup elementów, które wzmacniają się nawzajem i tworzą pozycję."
      />
    </div>
  </div>

  <!-- ===================== STAT CIRCLE ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="stat-circle">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.15 StatCircle</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/common/StatCircle.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Big proof-point value on a 270px yellow disk + caption below. Composes <code class="font-mono">IconBadge</code> (lg/accent) with the value centred over it via absolute positioning.</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">value</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Centred value (e.g. "5x", "90%")</td></tr>
        <tr><td class="py-tight pr-relaxed">caption</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Body copy underneath</td></tr>
      </tbody>
    </table>

    <div class="p-comfortable bg-surface-light flex flex-wrap gap-comfortable">
      <StatCircle value="5x" caption="wyższa penetracja rynku dla marek znacząco innych" />
      <StatCircle value="2x" caption="wyższa skłonność klientów do zapłaty wyższej ceny" />
      <StatCircle value="90%" caption="wpływ inności na predyspozycję do zapłaty premium" />
    </div>
  </div>

  <!-- ===================== STATS GRID ===================== -->
  <div class="py-comfortable px-page-gutter border-t border-outline-light" id="stats-grid">
    <h2 class="font-body text-headline-sm leading-normal text-on-surface-dark mb-compact">2.16 StatsGrid</h2>
    <code class="font-mono text-label text-on-surface-dark block mb-compact">src/components/common/StatsGrid.astro</code>
    <p class="font-body text-label-sm text-text-muted mb-relaxed">Horizontal row of <code class="font-mono">StatCircle</code> with an optional source footnote. Desktop: items distributed along the row via <code class="font-mono">justify-between</code>. Mobile: stacked vertically.</p>

    <table class="font-body text-label w-full mb-comfortable">
      <thead><tr class="text-left border-b border-outline-light">
        <th class="py-tight pr-relaxed text-text-muted font-normal">Prop</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Typ</th>
        <th class="py-tight pr-relaxed text-text-muted font-normal">Default</th>
        <th class="py-tight text-text-muted font-normal">Opis</th>
      </tr></thead>
      <tbody class="font-mono text-label-sm">
        <tr class="border-b border-outline-subtle"><td class="py-tight pr-relaxed">items</td><td class="py-tight pr-relaxed text-text-muted">{"{ value, caption }[]"}</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Stats list (typically 3)</td></tr>
        <tr><td class="py-tight pr-relaxed">source</td><td class="py-tight pr-relaxed text-text-muted">string</td><td class="py-tight pr-relaxed text-text-muted">—</td><td class="py-tight font-body text-text-muted">Optional small footnote under the row</td></tr>
      </tbody>
    </table>

    <div class="p-comfortable bg-surface-light">
      <StatsGrid
        items={[
          { value: "5x", caption: "wyższa penetracja rynku dla marek znacząco innych" },
          { value: "2x", caption: "wyższa skłonność klientów do zapłaty wyższej ceny" },
          { value: "90%", caption: "wpływ inności na predyspozycję do zapłaty premium" },
        ]}
        source="Źródło: Blueprint for Brand Growth, Kantar 2024."
      />
    </div>
  </div>

</PreviewLayout>

<script>
  const bgMap: Record<string, string> = {
    light: 'bg-surface-light', muted: 'bg-surface-muted',
    dark: 'bg-surface-dark', accent: 'bg-surface-accent',
  };
  const allBg = Object.values(bgMap);
  const on = 'bg-on-surface-dark text-on-surface-light';
  const off = 'hover:bg-surface-muted';

  document.addEventListener('click', (e) => {
    const btn = (e.target as HTMLElement).closest('[data-dim]') as HTMLElement;
    if (!btn) return;
    const comp = btn.closest('.ds-component') as HTMLElement;
    if (!comp) return;

    const dim = btn.dataset.dim!;   // "surface", "icon", "indicator"
    const val = btn.dataset.value!;  // "light", "arrow", "with", etc.

    // Update active state on switcher buttons for this dimension
    comp.querySelectorAll(`[data-dim="${dim}"]`).forEach((b) => {
      b.classList.remove(...on.split(' '));
      b.classList.add(...off.split(' '));
    });
    btn.classList.remove(...off.split(' '));
    btn.classList.add(...on.split(' '));

    if (dim === 'surface') {
      // Show matching panel, hide others
      comp.querySelectorAll('.ds-panel').forEach((panel) => {
        const el = panel as HTMLElement;
        el.classList.toggle('hidden', el.dataset.panelSurface !== val);
      });
    } else if (dim === 'current') {
      // ReportAreasMenu: flip data-current on matching <li data-area-number="N">
      comp.querySelectorAll('[data-area-number]').forEach((li) => {
        const el = li as HTMLElement;
        el.dataset.current = el.dataset.areaNumber === val ? 'true' : 'false';
      });
    } else {
      // Toggle rows by data-row-{dim}
      comp.querySelectorAll(`[data-row-${dim}]`).forEach((row) => {
        const el = row as HTMLElement;
        el.classList.toggle('hidden', el.getAttribute(`data-row-${dim}`) !== val);
      });
    }
  });
</script>
